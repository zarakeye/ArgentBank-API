services:
  - type: web
    name: bank-argent-backend
    envVars:
      - key: NODE
        value: node
      - key: PORT
        value: 10000
      - key: NODE_ENV
        value: production
    plan: free
    
build:
  npmInstall:
    cacheVersion: v1